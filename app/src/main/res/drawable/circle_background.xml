<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Cor de preenchimento interna: transparente, para que a imagem ou cor de fundo da ImageView apareça -->
    <solid android:color="@android:color/transparent"/>

    <!-- A Borda (Stroke): Define a cor preta e a espessura para o contorno circular -->
    <stroke
        android:width="3dp"
        android:color="@android:color/black" />

</shape>